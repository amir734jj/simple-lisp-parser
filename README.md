# simple-lisp-parser

Very basic Lisp parser using Scala parser combinators

It can currently handle:

- arithmetic operations: `+ -`
- logical operations: `< >`
- conditional
- function definition
- function invocation